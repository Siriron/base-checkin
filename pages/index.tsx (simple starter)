export default function Home() {
  return (
    <div style={{ textAlign: "center", marginTop: "50px" }}>
      <h1>Base Daily Check-In</h1>
      <p>Connect your wallet and check-in.</p>
      <button onClick={() => alert("SIWE + Check-in functionality coming!")}>
        Connect Wallet & Check-in
      </button>
    </div>
  );
}
